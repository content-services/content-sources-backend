# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
release/
tmp/
# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
.idea
.env
configs/config.yaml
# Allow disabled allias for config
configs/config_disabled.yaml
.venv

# Visual Studio Code settings
.vscode/**
!.vscode/settings.json
!.vscode/launch.json

# Ignore temporal binary for testing generated by VSCode
./**/__debug_bin

# outputs from build
Containerfile

# Test main files:
cmd/test/

# Prometheus
prometheus/data
configs/prometheus.yaml

# Pulp Oci Images
compose_files/pulp/pulp-oci-images

# Bridge to Kubernetes
KubernetesLocalProcessConfig.yaml

#playwright ignores 
node_modules/
test-results/
playwright-report/
playwright-ctrf/
blob-report/
playwright/.cache/
.auth
bin
.DS_Store
#ignore the cloned repo
content-sources-frontend

# local dev certs for testing pulp cert auth
compose_files/pulp/assets/certs/dev_certs
