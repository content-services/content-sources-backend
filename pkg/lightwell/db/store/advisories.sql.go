// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: advisories.sql

package store

import (
	"context"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const countAdvisoriesByRepo = `-- name: CountAdvisoriesByRepo :one
SELECT COUNT(*)::bigint AS total
FROM lightwell_advisories la
WHERE la.repository_configuration_uuid = $1::uuid
`

func (q *Queries) CountAdvisoriesByRepo(ctx context.Context, repositoryConfigUuid uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countAdvisoriesByRepo, repositoryConfigUuid)
	var total int64
	err := row.Scan(&total)
	return total, err
}

const listAdvisories = `-- name: ListAdvisories :many
SELECT
    la.uuid,
    la.advisory_id,
    la.severity,
    la.severity_order,
    la.details,
    la.reference_urls,
    la.package_name,
    la.fixed_versions,
    la.repo_name,
    la.repository_configuration_uuid,
    la.created_at,
    COUNT(*) OVER() AS total_count
FROM lightwell_advisories la
WHERE 1=1
    AND (
        $1::uuid IS NULL
        OR la.repository_configuration_uuid = $1::uuid
    )
    AND (
        $2::text IS NULL
        OR la.repo_name = $2::text
    )
    AND (
        $3::text IS NULL
        OR la.package_name ILIKE '%' || $3::text || '%'
    )
    AND (
        $4::smallint IS NULL
        OR la.severity_order >= $4::smallint
    )
    AND (
        $5::text IS NULL
        OR la.advisory_id = $5::text
    )
ORDER BY la.severity_order DESC, la.created_at DESC
LIMIT $7 OFFSET $6
`

type ListAdvisoriesParams struct {
	RepositoryConfigUuid pgtype.UUID `json:"repository_config_uuid"`
	RepoName             *string     `json:"repo_name"`
	PackageName          *string     `json:"package_name"`
	SeverityMin          pgtype.Int2 `json:"severity_min"`
	CveID                *string     `json:"cve_id"`
	PageOffset           int32       `json:"page_offset"`
	PageLimit            int32       `json:"page_limit"`
}

type ListAdvisoriesRow struct {
	Uuid                        uuid.UUID `json:"uuid"`
	AdvisoryID                  string    `json:"advisory_id"`
	Severity                    string    `json:"severity"`
	SeverityOrder               int16     `json:"severity_order"`
	Details                     string    `json:"details"`
	ReferenceUrls               []string  `json:"reference_urls"`
	PackageName                 string    `json:"package_name"`
	FixedVersions               []string  `json:"fixed_versions"`
	RepoName                    string    `json:"repo_name"`
	RepositoryConfigurationUuid uuid.UUID `json:"repository_configuration_uuid"`
	CreatedAt                   time.Time `json:"created_at"`
	TotalCount                  int64     `json:"total_count"`
}

func (q *Queries) ListAdvisories(ctx context.Context, arg ListAdvisoriesParams) ([]ListAdvisoriesRow, error) {
	rows, err := q.db.Query(ctx, listAdvisories,
		arg.RepositoryConfigUuid,
		arg.RepoName,
		arg.PackageName,
		arg.SeverityMin,
		arg.CveID,
		arg.PageOffset,
		arg.PageLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListAdvisoriesRow{}
	for rows.Next() {
		var i ListAdvisoriesRow
		if err := rows.Scan(
			&i.Uuid,
			&i.AdvisoryID,
			&i.Severity,
			&i.SeverityOrder,
			&i.Details,
			&i.ReferenceUrls,
			&i.PackageName,
			&i.FixedVersions,
			&i.RepoName,
			&i.RepositoryConfigurationUuid,
			&i.CreatedAt,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAdvisoriesByCveID = `-- name: ListAdvisoriesByCveID :many
SELECT
    la.package_name,
    la.fixed_versions,
    la.repo_name,
    la.severity
FROM lightwell_advisories la
WHERE la.advisory_id = $1::text
`

type ListAdvisoriesByCveIDRow struct {
	PackageName   string   `json:"package_name"`
	FixedVersions []string `json:"fixed_versions"`
	RepoName      string   `json:"repo_name"`
	Severity      string   `json:"severity"`
}

func (q *Queries) ListAdvisoriesByCveID(ctx context.Context, cveID string) ([]ListAdvisoriesByCveIDRow, error) {
	rows, err := q.db.Query(ctx, listAdvisoriesByCveID, cveID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListAdvisoriesByCveIDRow{}
	for rows.Next() {
		var i ListAdvisoriesByCveIDRow
		if err := rows.Scan(
			&i.PackageName,
			&i.FixedVersions,
			&i.RepoName,
			&i.Severity,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAdvisoriesByPackage = `-- name: ListAdvisoriesByPackage :many
SELECT
    la.advisory_id,
    la.severity,
    la.severity_order,
    la.details,
    la.fixed_versions,
    la.repo_name
FROM lightwell_advisories la
WHERE la.package_name = $1::text
ORDER BY la.severity_order DESC, la.created_at DESC
`

type ListAdvisoriesByPackageRow struct {
	AdvisoryID    string   `json:"advisory_id"`
	Severity      string   `json:"severity"`
	SeverityOrder int16    `json:"severity_order"`
	Details       string   `json:"details"`
	FixedVersions []string `json:"fixed_versions"`
	RepoName      string   `json:"repo_name"`
}

func (q *Queries) ListAdvisoriesByPackage(ctx context.Context, packageName string) ([]ListAdvisoriesByPackageRow, error) {
	rows, err := q.db.Query(ctx, listAdvisoriesByPackage, packageName)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListAdvisoriesByPackageRow{}
	for rows.Next() {
		var i ListAdvisoriesByPackageRow
		if err := rows.Scan(
			&i.AdvisoryID,
			&i.Severity,
			&i.SeverityOrder,
			&i.Details,
			&i.FixedVersions,
			&i.RepoName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
